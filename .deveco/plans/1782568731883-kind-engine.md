---
name: fix-alignment-and-validation
overview: 修复布局对齐 + SourceChecker API 兼容
todos:
- task: "修复 BookSourcePage 行内布局：编辑/菜单/checkbox 垂直居中对齐"
  status: pending
- task: "修复 SourceChecker API：适配当前 ExploreEngine/SourceExecutor/BookSourceBookInfo 接口"
  status: pending
- task: "BookSourcePage 接入 SourceChecker：校验按钮 + 进度显示"
  status: pending
- task: "Build + Deploy + Test"
  status: pending
---

# 布局对齐 + 校验功能修复

## 1. 布局对齐

问题：✎按钮、⋮按钮、Checkbox 在每行中不在同一水平线。

修复：给 Row 添加 `alignItems: VerticalAlign.Center`

## 2. SourceChecker API 修复

当前错误：
- `ExploreEngine.parseKinds` — 当前版本方法名可能不同
- `ExploreEngine.fetchKindBooks` — 同上
- `globalSourceExecutor.getBookInfo` — 当前版本可能不存在
- `BookSourceBookInfo.tocUrl` — 接口可能缺失该字段

策略：检查当前 API，移除不存在的方法调用，保留搜索+目录+正文三个核心校验项。

## 3. BookSourcePage 接入

- 添加 `@State checking: boolean` 和 `@State checkProgress: string`
- 校验按钮调用 `startCheck()`
- 列表上方显示进度条

